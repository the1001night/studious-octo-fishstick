# Backend API с аутентификацией

Полнофункциональный backend на Node.js с Express, MongoDB и JWT аутентификацией.

## 🚀 Быстрый старт

### Установка зависимостей
```bash
npm install
```

### Настройка переменных окружения
Создайте файл `.env` в корне проекта:
```
PORT=5000
MONGODB_URI=mongodb://localhost:27017/auth_db
JWT_SECRET=your_super_secret_jwt_key_here_change_in_production
NODE_ENV=development
```

### Установка и запуск MongoDB

**Для Windows:**
1. Скачайте MongoDB Community Server с [официального сайта](https://www.mongodb.com/try/download/community)
2. Установите MongoDB в папку по умолчанию (обычно `C:\Program Files\MongoDB\Server\7.0\`)
3. Создайте папку для данных: `mkdir C:\data\db`
4. Запустите MongoDB:
   ```cmd
   "C:\Program Files\MongoDB\Server\7.0\bin\mongod.exe" --dbpath "C:\data\db"
   ```
5. Или добавьте MongoDB в PATH системы и используйте просто `mongod`

**Быстрая установка через Chocolatey (рекомендуется):**
```cmd
choco install mongodb
```

**Для других ОС:**
Следуйте инструкциям на [официальном сайте MongoDB](https://docs.mongodb.com/manual/installation/)

**Запуск MongoDB:**
```bash
mongod
```

**Альтернатива: MongoDB Atlas (облачная база данных)**
1. Зарегистрируйтесь на [MongoDB Atlas](https://www.mongodb.com/atlas)
2. Создайте кластер
3. Получите connection string и замените `MONGODB_URI` в `.env` файле

Если MongoDB не запущена, сервер всё равно будет работать, но функции аутентификации будут недоступны.

### Запуск сервера

**Вариант 1: Ручной запуск**
```bash
# Сначала запустите MongoDB в отдельном терминале
mongod

# Затем в другом терминале запустите сервер
# Режим разработки (с автоматической перезагрузкой)
npm run dev

# Продакшн режим
npm start
```

**Вариант 2: Автоматический запуск (Windows)**
```cmd
# Запуск MongoDB и сервера одновременно
start.bat

# Или с помощью PowerShell
powershell -ExecutionPolicy Bypass -File start.ps1
```

Сервер запустится на `http://localhost:5000`

**Примечание:** Сервер может работать без MongoDB, но функции регистрации и аутентификации будут недоступны до запуска базы данных.

## 📋 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Получить информацию о текущем пользователе (требует авторизации)
- `POST /api/auth/logout` - Выход из системы (требует авторизации)

### Управление пользователями
- `GET /api/users` - Получить всех пользователей (только админ)
- `GET /api/users/:id` - Получить пользователя по ID (только админ)
- `PUT /api/users/profile` - Обновить профиль (требует авторизации)
- `PUT /api/users/change-password` - Изменить пароль (требует авторизации)
- `DELETE /api/users/:id` - Удалить пользователя (только админ)

## 🔐 Аутентификация

Для защищенных маршрутов необходимо передавать JWT токен в заголовке:
```
Authorization: Bearer <your_jwt_token>
```

## 📝 Форматы запросов

### Регистрация
```json
POST /api/auth/register
{
  "name": "Иван Иванов",
  "email": "ivan@example.com",
  "password": "password123"
}
```

### Вход
```json
POST /api/auth/login
{
  "email": "ivan@example.com",
  "password": "password123"
}
```

### Обновление профиля
```json
PUT /api/users/profile
Authorization: Bearer <token>
{
  "name": "Иван Петров",
  "email": "ivan.petrov@example.com"
}
```

## 🛠 Технологии

- **Node.js** - JavaScript runtime
- **Express.js** - Web framework
- **MongoDB** - NoSQL база данных
- **Mongoose** - ODM для MongoDB
- **JWT** - JSON Web Tokens для аутентификации
- **bcryptjs** - Хэширование паролей
- **express-validator** - Валидация данных

## 📁 Структура проекта

```
├── config/
│   └── database.js          # Подключение к MongoDB
├── middleware/
│   ├── auth.js             # JWT аутентификация
│   └── validation.js       # Валидация данных
├── models/
│   └── User.js             # Модель пользователя
├── routes/
│   ├── auth.js             # Маршруты аутентификации
│   └── users.js            # Маршруты пользователей
├── server.js               # Главный файл сервера
├── package.json
└── README.md
```

## 🔒 Безопасность

- Пароли хэшируются с помощью bcrypt
- JWT токены для аутентификации
- Валидация всех входных данных
- CORS включен для кросс-доменных запросов
- Обработка ошибок с подробными сообщениями

## 📊 Статусы ответов API

- `200` - Успешный запрос
- `201` - Ресурс создан
- `400` - Ошибка валидации
- `401` - Не авторизован
- `403` - Доступ запрещен
- `404` - Ресурс не найден
- `500` - Внутренняя ошибка сервера

## 🧪 Тестирование API

Вы можете использовать Postman, Insomnia или любой другой HTTP клиент для тестирования API.

Пример создания первого пользователя (админа):
1. Отправьте POST запрос на `/api/auth/register`
2. Затем вручную измените роль пользователя в MongoDB на 'admin' для доступа к админ-функциям
